# lark-doc media download

下载飞书文档里的图片、附件或画板缩略图。

```bash
lark-cli docs +media-download --doc "文档URL或token" --media-id <media_id> --output <relative_path> --as user
```

画板缩略图只能用 download，不要用 preview。
