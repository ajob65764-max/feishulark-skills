# lark-markdown create

在飞书 Drive 中创建 Markdown 文件。

```bash
lark-cli markdown +create --name "note.md" --content "# 标题" --as user
lark-cli markdown +create --name "note.md" --content @local.md --as user
```

文件名必须包含 `.md` 后缀。
