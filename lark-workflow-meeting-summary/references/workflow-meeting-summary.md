# workflow meeting summary

会议纪要汇总工作流参考。

## 流程

1. 用 `vc +search` 查询时间范围内会议。
2. 用 `vc +detail` 获取 note_id / minute_token。
3. 用 `note +detail` 或 `minutes +detail` 获取产物。
4. 必要时用 `lark-doc` 读取正文。
5. 汇总会议主题、决策、待办和负责人。
6. 按用户要求输出到消息、文档或表格。
