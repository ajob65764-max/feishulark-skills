# lark-drive search

搜索飞书云空间资源。

```bash
lark-cli drive +search --query "关键词" --as user
```

## 适用场景

- 用户只知道文件名、文档标题或关键词。
- 需要查找 doc、sheet、base、wiki、folder 等资源。
- 需要先定位 token 再切到对应 skill。

## 规则

搜索结果多条时，展示标题、类型、更新时间和链接，让用户确认目标。
